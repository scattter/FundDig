# fund-plan-backend

NestJS backend skeleton for fund plan management.

Quick start

- copy `.env.example` to `.env` and fill DB credentials
- npm install
- npm run start:dev

API

- POST /plans  { name, description?, rules? }
- GET /plans
- GET /plans/:id
