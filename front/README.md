# fund-plan-front

Front-end scaffold using rsbuild + react + typescript + biome.

Quick start

- npm install
- npm run dev

Notes

- The app expects the backend to be available at /api (use a proxy or run both and setup CORS in backend).
